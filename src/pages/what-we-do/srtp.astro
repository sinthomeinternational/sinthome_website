---
import PageLayout from "../../layouts/PageLayout.astro";
import H1 from "../../components/ui/H1.astro";
import EventCardsWithModal from "../../components/srtp/EventCardsWithModal";
import { getCollection, render } from 'astro:content';
import { getLanguageFromURL } from "../../lib/translations";

// Get all SRTP events from content collection
const srtpEvents = await getCollection('srtpEvents');

// Sort events by date (most recent first) and include body content
const sortedEvents = srtpEvents
  .map(event => ({
    ...event,
    body: event.body || '' // Include the markdown body content
  }))
  .sort((a, b) => {
    return new Date(b.data.date).getTime() - new Date(a.data.date).getTime();
  });

// Get current language
const lang = getLanguageFromURL(Astro.url);
---

<PageLayout title="S.R.T.P. - Sinthome" useContentLayout={true}>
    <!-- Enhanced Title Structure -->
    <div class="text-center mb-12">
      <h1 class="text-6xl font-bold text-white mb-4 tracking-wider">S.R.T.P.</h1>
      <h2 class="text-2xl text-red-400 font-medium tracking-wide">Sinthome Revolutionary Theoretical Practice</h2>
      <div class="w-32 h-1 bg-gradient-to-r from-red-600 to-red-800 mx-auto mt-6"></div>
    </div>

    <div class="space-y-16">
      <!-- Activities Section -->
      <section>
        <h2 class="text-2xl font-bold text-white mb-6 text-center">Activities</h2>

        <!-- Compact Activities Card -->
        <div class="relative group">
          <!-- Background glow effect -->
          <div class="absolute -inset-0.5 bg-gradient-to-r from-red-900/10 via-red-800/5 to-red-900/10 rounded-lg blur opacity-75 group-hover:opacity-100 transition duration-500"></div>

          <!-- Main card -->
          <div class="relative bg-gradient-to-br from-zinc-900/90 to-zinc-800/70 border border-zinc-700/50 rounded-lg p-6 backdrop-blur-sm">
            <!-- Card header with schedule inline -->
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-4">
              <div class="flex items-center mb-2 sm:mb-0">
                <div class="w-2 h-2 bg-red-500 rounded-full mr-3"></div>
                <h3 class="text-xl font-semibold text-red-400">Lectures & Seminars in Boston</h3>
              </div>
              <div class="flex items-center text-sm">
                <span class="text-red-400 font-bold mr-2">📅</span>
                <span class="text-zinc-200 font-semibold">Every Saturday</span>
                <span class="text-zinc-400 ml-2 hidden sm:inline">• In-person gatherings</span>
              </div>
            </div>

            <!-- Intro text -->
            <p class="text-zinc-300 text-sm mb-4">
              We come together for a series of in-person seminars featuring:
            </p>

            <!-- Compact Activities list -->
            <div class="grid gap-3 mb-5">
              <div class="flex items-start group/item">
                <div class="w-1.5 h-1.5 bg-red-500 rounded-full mr-3 mt-1.5 flex-shrink-0 group-hover/item:bg-red-400 transition-colors"></div>
                <div>
                  <span class="text-zinc-200 font-medium">Philosophical Lectures:</span>
                  <span class="text-zinc-400 text-sm ml-1">Deep dives into philosophy, political economy, and related disciplines</span>
                </div>
              </div>

              <div class="flex items-start group/item">
                <div class="w-1.5 h-1.5 bg-red-500 rounded-full mr-3 mt-1.5 flex-shrink-0 group-hover/item:bg-red-400 transition-colors"></div>
                <div>
                  <span class="text-zinc-200 font-medium">Textual Analysis:</span>
                  <span class="text-zinc-400 text-sm ml-1">Close readings of foundational theoretical texts</span>
                </div>
              </div>

              <div class="flex items-start group/item">
                <div class="w-1.5 h-1.5 bg-red-500 rounded-full mr-3 mt-1.5 flex-shrink-0 group-hover/item:bg-red-400 transition-colors"></div>
                <div>
                  <span class="text-zinc-200 font-medium">Praxis Reflection:</span>
                  <span class="text-zinc-400 text-sm ml-1">Collective discussion on issues emerging from our practices</span>
                </div>
              </div>
            </div>

            <!-- Compact Mission statement -->
            <div class="border-l-3 border-red-600/50 pl-4 py-2 bg-gradient-to-r from-zinc-800/20 to-transparent rounded-r">
              <p class="text-zinc-300 text-sm leading-6">
                These gatherings aim to equip community members with a solid grounding in leftist theory, help build theoretical consensus, and cultivate new critical insights. Through this process, we seek to maintain a <span class="text-red-300 font-medium">dynamic and reciprocal relationship between theory and practice</span>.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Past Events Section -->
      <section>
        <h2 class="text-3xl font-bold text-white mb-10 text-center">Past Events</h2>
        <p class="text-zinc-400 text-center mb-8">
          Click on any event card to view more details. Our archive includes lectures, seminars, and workshops on revolutionary theory and practice.
        </p>

        <!-- Event Cards with Modal -->
        <EventCardsWithModal events={sortedEvents} lang={lang} client:load />
      </section>

      <!-- Call to Action -->
      <section class="mt-16 text-center bg-zinc-900/50 p-12 rounded-lg border border-zinc-800">
        <div class="max-w-2xl mx-auto">
          <h3 class="text-3xl font-bold text-white mb-4">Join Our Community</h3>
          <p class="text-zinc-400 text-lg mb-8">
            Join our intellectual community and be part of the interrogation that opens the future.
          </p>
        </div>

        <a
          href="/contact"
          class="inline-flex items-center px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-red-600/25"
        >
          Contact Us
          <span class="ml-2">→</span>
        </a>
      </section>
    </div>
</PageLayout>
